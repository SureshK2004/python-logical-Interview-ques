#write a simple to add two number using lambda

#syntax: funcname=lambda(keyword) args:output

add=lambda x,y:x+y
print(add(4,5))

#write a function to twice the number

twice=lambda x:x*2
print(twice(5))

#write a program to square of the number 
square=lambda x:x**2
print(square(7)) 